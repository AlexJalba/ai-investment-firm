version: "3.9"

services:
  firm:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    command: python -m src.cli dashboard

  # Optional: OpenTelemetry collector (remove if not needed)
  # otel-collector:
  #   image: otel/opentelemetry-collector-contrib:latest
  #   ports:
  #     - "4317:4317"
  #   volumes:
  #     - ./otel-collector-config.yml:/etc/otelcol-contrib/config.yaml
